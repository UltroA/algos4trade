---
name: Bug report
about: Something in the benchmark suite, an algorithm, or a script is broken
title: ""
labels: bug
assignees: ""
---

**Before posting: double-check this issue text and any pasted logs/commands
contain no `T_INVEST_TOKEN` or `Authorization: Bearer ...` value - see
`SECURITY.md`.**

## What happened

<!-- What you ran, and what went wrong. -->

## What you expected

## How to reproduce

```bash
# the exact command(s) you ran
```

## Environment

- OS:
- Python version (`python3 --version`):
- Install method: `pip install -r requirements.txt` / `pip install -e .` / `pip install -e ".[news]"`
- Relevant package versions if you changed anything from `requirements.txt`/`pyproject.toml`:

## Logs / traceback

```
paste here
```
