<!--
Before opening: read CONTRIBUTING.md, especially "AI-assisted code" if any
part of this PR was AI-generated, and "Commits and sign-off" for the DCO
requirement. Double-check this description and any pasted logs contain no
T_INVEST_TOKEN / Authorization header value - see SECURITY.md.
-->

## What this changes and why

## Test plan

<!-- e.g. "ran `pytest`", "ran `PYTHONPATH=src python src/algorithms/<file>.py`
     smoke test", "ran scripts/run_benchmarks.py end to end". Say explicitly
     if something wasn't tested (e.g. a live-market-simulator change that
     needs real market hours to fully exercise) rather than leaving it blank. -->

## Checklist

- [ ] `README.md` and `README_RU.md` updated together, if this changes anything either documents
- [ ] Commits are signed off (`git commit -s`) per `CONTRIBUTING.md`
- [ ] If any code here is AI-assisted, that's noted per `CONTRIBUTING.md`'s "AI-assisted code" section
- [ ] `pytest` passes locally (`pip install -e ".[dev]"`)
